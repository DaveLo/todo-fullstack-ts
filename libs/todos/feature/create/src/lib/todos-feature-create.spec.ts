import { todosFeatureCreate } from "./todos-feature-create";

describe("todosFeatureCreate", () => {
  it("should work", () => {
    expect(todosFeatureCreate()).toEqual("todos-feature-create");
  });
});
